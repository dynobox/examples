prefix_rule(
  pattern = ["./hunk"],
  decision = "allow",
  justification = "This fixture is a deterministic Hunk CLI in an isolated Dynobox workspace.",
)

prefix_rule(
  pattern = ["printf"],
  decision = "allow",
  justification = "This fixture pipes deterministic comment JSON to the local Hunk CLI.",
)
